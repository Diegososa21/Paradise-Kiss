{
  "name": "paradise-kiss-inventory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vanilla HTML, CSS und JavaScript. Django als separates Backend.",
  "scripts": {
    "start": "python -m http.server 4200 --bind 127.0.0.1 --directory frontend",
    "test": "node --test tests/*.test.js"
  }
}
